using System;
using System.Collections.Generic;
using System.Text;

namespace ThirtyFunctinalAndAlgorithmProgram
{
    class SumAllArrayElement
    {
        public void isarry(int[] a,int n)
        {
            int sum = 0;
           for(int i = 0; i < a.Length; i++)
            {
                 sum = sum + a[i];
            }
        }
    }
}
